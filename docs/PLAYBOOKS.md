# PLAYBOOKS

20 local templates in `src/runtime/catalog.ts` + `catalog-playbooks.ts`.
They are optimization templates. The engineering loop can run against any inspected repository.
Northstar is a fixture, not the production target.
