# SANDBOX

IMPLEMENTED: Linux namespaces + chroot + OverlayFS via `services/sandbox/enter.sh`.
If tools are missing: `SANDBOX_UNAVAILABLE` / exit 126. No host fallback.
Firecracker / remote: UNSUPPORTED (`REMOTE_EXECUTION_UNAVAILABLE`).
