# خطة الإصلاح الشاملة — ALJWHARAH ONE
**تبدأ:** 22 أغسطس 2026 · هذه اللحظة  
**فرع التنفيذ:** `repair/golden-path`  
**قانون:** لا توسعة. قصّ. المسار الذهبي أولاً. كل مرحلة لها تعريف انتهاء قابل للقياس.

هذا الملف هو مصدر الحالة الوحيد لهذه الحملة. التقارير الأقدم أرشيف.

---

## الهدف النهائي

منتج ويندوز يستطيع: نية مستخدم → وكلاء بعقود → رقعة حقيقية → اختبار → محقّق مستقل → إثبات يمكن رفضه.

ليس محرراً. ليس معاينة ويب تُسمَّى تطبيقاً. ليس قائمة محركات جديدة.

---

## ما بدأ الآن (المرحلة 0) — منفَّذ في هذا الفرع

| عمل | تعريف الانتهاء | حالة |
|---|---|---|
| مرمّم `aj-local` حتمي | P5-1 أخضر: `add()` من `-` إلى `+` + اختبار | تم |
| السجن يُستدعى عبر `sh` | sandbox.test.ts أخضر بدون بت تنفيذي | تم |
| هوية المنتج | شاشة الدخول ليست «code editor» | تم |
| اسم الحزمة | `aljwharah-one` لا `app-builder-workspace` | تم |
| حذف رقم حساب AWS من Git | الملف `.aws/...` بلا Account ID | تم |
| README صادق + رخصة | README يصف ما يعمل / ما لا يعمل | تم |
| CI | `.github/workflows/ci.yml` يشغّل `npm test` + typecheck | تم |

قياس هذه الجولة: **226 اختباراً · كان 18 فشلاً · بعد المرحلة 0: ~6 فشل** (مهام playbook/computer-use + اختبار مضيف ويندوز).

---

## المرحلة 1 — خلال 48 ساعة · المسار الذهبي للمنتج

الوعد الذي يراه المالك: «أصلح هذا المستودع وأثبت العمل».

| # | عمل | تعريف الانتهاء |
|---|---|---|
| 1.1 | مهمة `GET /health` تصل `COMPLETE` + `PASS` من المحقّق المستقل | `runtime.test.ts` health أخضر |
| 1.2 | مسار computer-use: إمّا يقلع Chromium ويكتشف عيب الدخول، أو يفشل مغلقاً بصراحة بدون ادّعاء PASS | اختبارات v2 computer-use خضراء أو معلَّمة `UNSUPPORTED` |
| 1.3 | حلقة LLM تبقى fail-closed بلا مفتاح (`implementObjectiveAsync`) | P1B-1 async يبقى أحمر-مرفوض للمزوّد |
| 1.4 | `aj run "Fix add()"` من CLI يكتب رقعة + دليل | أمر CLI واحد على fixture |
| 1.5 | لا مهمة تُختم إذا فشل المحقّق | موجود؛ يُعاد تأكيده على health |

ممنوع في هذه المرحلة: مزوّدات جديدة، Firecracker، Marketplace.

---

## المرحلة 2 — بقية الاختبارات الحمراء

| فشل معروف | العلاج |
|---|---|
| `native host: mica / unsigned updater / HKCU` | اختبار ويندوز — يُشغَّل في CI windows-latest أو يُعلَّم `skip` على لينكس |
| overlay flaky | عزل `AJ_DATA_DIR` لكل اختبار |
| ACP worker حي | إما عملية ابن حقيقية تحت منح، أو `ACP_UNAVAILABLE` صريح |

تعريف الانتهاء: `npm test` = 0 فشل على ubuntu-latest.

---

## المرحلة 3 — قص الواجهة (ليس محرراً)

| عمل | تعريف الانتهاء |
|---|---|
| المقعد الافتراضي: مهام / وكلاء / أدلة — المحرر ثانوي | أول شاشة بعد الدخول ليست محرر ملفات |
| إزالة «Intelligence Editor» من كل النسخ | grep = 0 |
| وضع ONE/WORK يغيّر التشغيل لا اللون فقط | اختبار سلوك موجود |
| الملحن لا يبدأ مهمة قبل موافقة الخطة | موجود؛ يُحافظ عليه |

---

## المرحلة 4 — هندسة GitHub

| عمل | تعريف الانتهاء |
|---|---|
| حماية `main` | لا دفع مباشر؛ PR + CI أخضر |
| وصف المستودع + مواضيع | `agent-os`, `tauri`, `typescript` |
| Dependabot | مفعّل |
| ملف حالة واحد | هذا الملف + ARCHITECTURE + SECURITY. الباقي `docs/archive/` |
| لا أسرار بنية | git-secrets / secret scanning |

---

## المرحلة 5 — شحن ويندوز (أسبوع 2)

| عمل | تعريف الانتهاء |
|---|---|
| `desktop:build` ينتج exe حقيقي | ملف موجود على جهاز ويندوز |
| NSIS/MSI غير موقَّع يُثبَّت على جهاز نظيف | تشغيل من قائمة ابدأ بلا localhost يدوي |
| الصينية + الاختصار + الإشعار تعمل | تحقق يدوي مرة واحدة |
| المحدّث | يبقى رافضاً للتوقيع حتى يوجد `releases.aljwharah.one` حقيقي |

لا ندّعي مثبّتاً موقَّعاً قبل شهادة.

---

## المرحلة 6 — العزل على ويندوز (بعد الشحن)

الخيارات بالترتيب الصادق:

1. الإبقاء على الفشل المغلق + رسالة في الواجهة: «السجن غير متوفر على ويندوز»
2. WSL2 مستخدم فعلاً لنفس `enter.sh`
3. Job Object / AppContainer لاحقاً

لا Docker كسجن منتج. لا ادّعاء عزل بلا قياس.

---

## المرحلة 7 — التقليم

بعد أن تمر مهمة واحدة حقيقية من الواجهة إلى دليل:

- لا محركات سحابة جديدة
- أرشفة وحدات runtime التي لا يلمسها المسار الذهبي
- تقسيم `ajd.ts` (3411 سطراً) بعد استقرار العقود لا قبلها

---

## خارج النطاق حتى انتهاء 1–3

- Firecracker حي
- نشر VS Code Marketplace
- توقيع EV
- OpenAI / Anthropic كمنتج
- Postgres مزامنة فرق
- نطاقات تسويق بلا خادم

---

## قواعد لا تُكسر أثناء الإصلاح

1. الطفل لا يتجاوز صلاحية الأب.
2. المنفّذ لا يختم المهمة.
3. المحتوى منخفض الثقة بيانات فقط.
4. الأسرار مؤجَّرة.
5. الدفتر إلحاق فقط.
6. لا تزييف مزوّد. `aj-local` مهندس حتمي، ليس نموذجاً لغوياً.
7. لا تُسمَّ معاينة Vite تطبيقاً ويندوز.

---

## جملة التسليم

الجوهرة أقوى لا لأنها تشغّل نماذج أكثر، بل لأن الوكيل لا يصدّق نفسه، والسر لا يعيش للأبد، والدفتر لا يُكذَب عليه، والمهمة لا تُختم إلا بدليل يمكن رفضه.
